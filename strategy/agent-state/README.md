# Agent State (JSON)

Agent runtime state is stored as one JSON file per role (`cpo.json`, `cto.json`, `cfo.json`) and should follow the HQ schema:

- https://github.com/fratei/creative-ware-hq/blob/main/framework/schemas/agent-state.json

This replaces the legacy markdown metrics file.
